name: publish-event-on-deployment-completion-to-zero
on:
  push:
    branches: [ main ]
  
  workflow_dispatch:
env: 
  GITHUB_CONTEXT: ${{ toJSON(github) }}

jobs:
  publish-event-on-deployment-completion-to-zero:
    name: Publish event on deployment completion to `ZERO`
    strategy:
      matrix:
        repos: ['PramodKumarYadav/zero', 'PramodKumarYadav/happium', 'PramodKumarYadav/pigeon']
    runs-on: ubuntu-latest
    steps:
      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.REPO_ACCESS_TOKEN_TO_TRIGGER_EVENTS }}
          repository: ${{ matrix.repos }}
          event-type: deployment-completed-event
          client-payload: '{"github": ${{ env.GITHUB_CONTEXT}}, "TEST_ENV": "DEVELOP" , "BASE_URL": "https://admin.develop.powertester.org/user/dashboard"}'
   
